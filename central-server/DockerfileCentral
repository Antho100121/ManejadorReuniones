FROM openjdk:17

WORKDIR /app

# Copiar todo el código fuente (modelo y servidorcentral)
COPY ./src/ ./src/
COPY employees.properties .

# Compilar las clases
RUN javac -d . src/servidorcentral/*.java src/modelo/*.java

EXPOSE 9090

CMD ["java", "servidorcentral.ServidorCentral"]
